Easiest way to build on windows 10. Requires only bash for windows.

Turn on bash for windows based on the directions here:

https://msdn.microsoft.com/en-us/commandline/wsl/install_guide

once in a bash command line. Change the directory to the root of the bootloader git repo.

Install make using the command

sudo apt-get install make

Build using the command:

make

